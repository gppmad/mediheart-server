version: '3.7'

services:

  web:
    build:
      context: .
      dockerfile: ./docker/prod/server/Dockerfile
    #command: python manage.py runserver 0.0.0.0:8000
    command: gunicorn -w 4 app_server.wsgi -b 0.0.0.0:8000
    #volumes:
    #  - ./app_server/:/app_server
    ports:
      - 8000:8000
    env_file:
      - ./env/prod/.env